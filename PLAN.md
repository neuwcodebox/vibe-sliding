# Implementation Plan

## Checklist

- [x] Create project guidance files: `AGENTS.md`, `PLAN.md`, `skills/`.
- [x] Create the design guide gallery under `designs/`.
- [x] Replace the starter Vite screen with the slide runtime.
- [x] Add fixed 1920x1080 stage scaling and slide navigation.
- [x] Add Edit Inspect Mode with hover target labels and one-line clipboard references.
- [x] Add at least three editable example slides.
- [x] Add Playwright screenshot capture scripts.
- [x] Update `README.md`, npm scripts, `.gitignore`, and TypeScript config.
- [x] Run `npm run typecheck`.
- [x] Run `npm run build`.
- [x] Commit completed implementation units with conventional commit messages.

## Notes

- Playwright browser execution is expected to be unavailable in this WSL environment, so screenshot scripts will be implemented but not validated through actual browser capture unless the environment allows it.
